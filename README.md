## Scholar Script Consult — Astro Website

Run locally:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build && npm run preview
```

Update contact details in `src/pages/index.astro` if needed:
- Phone: `phone` and `phoneDisplay`
- Email: `email`

Site uses plain CSS (no extra dependencies) and ships as a static site.


